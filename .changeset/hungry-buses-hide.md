---
'@nordeck/matrix-neoboard-react-sdk': minor
---

The polyline element will get simplified after it's loaded into the board and converted into a bezier curve with the help of the paper.js library. It won't change the way the path element is stored in the backend and only applies to the SVG.
